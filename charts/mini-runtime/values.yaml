# This chart uses the mini-runtime-base chart with latest image tags
# Values are passed to the subchart using the chart name prefix

akto-mini-runtime-base:
  mini_runtime:
    aktoApiSecurityRuntime:
      image:
        tag: latest

  threat_client:
    aktoApiSecurityThreatClient:
      image:
        tag: latest
